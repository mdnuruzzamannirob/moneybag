'use client';
import { AppButton, AppCard, AppPageHeader } from '@/components/app-ui';
import { ArrowRight, Check, Plus } from 'lucide-react';
const groups = ['Rahman Family', 'Travel Circle'];
const settlements = [['Tanvir Rahman', 'Ayesha Rahman', '৳ 4,800', 'Pending'], ['Nabila Rahman', 'Tanvir Rahman', '৳ 2,250', 'Pending'], ['Ayesha Rahman', 'Nabila Rahman', '৳ 1,200', 'Settled']];
export default function SettlementsPage() { return <div className="space-y-6"><AppPageHeader title="Settlements" description="Clear balances between members of the selected family group." actions={<><select className="h-9 rounded-lg border border-border bg-card px-3 text-sm" aria-label="Family group">{groups.map(g => <option key={g}>{g}</option>)}</select><AppButton><Plus className="size-4" /> Record settlement</AppButton></>} /><div className="grid gap-4 sm:grid-cols-3"><AppCard><p className="text-sm text-muted-foreground">You owe</p><p className="mt-2 text-2xl font-semibold text-danger">৳ 4,800</p></AppCard><AppCard><p className="text-sm text-muted-foreground">You are owed</p><p className="mt-2 text-2xl font-semibold text-success">৳ 2,250</p></AppCard><AppCard><p className="text-sm text-muted-foreground">Open settlements</p><p className="mt-2 text-2xl font-semibold">2</p></AppCard></div><AppCard padding="none"><div className="border-b border-border p-5"><h2 className="font-semibold">Settlement history</h2><p className="mt-1 text-sm text-muted-foreground">Rahman Family · all balances are group-scoped</p></div><div className="divide-y divide-border">{settlements.map(([from,to,amount,status]) => <div className="flex flex-wrap items-center justify-between gap-3 p-5" key={`${from}-${to}`}><div><p className="font-medium">{from} <ArrowRight className="mx-1 inline size-4 text-muted-foreground" /> {to}</p><p className="mt-1 text-xs text-muted-foreground">Recorded this month</p></div><div className="flex items-center gap-3"><span className="font-semibold">{amount}</span><span className={status === 'Settled' ? 'rounded-full bg-success-soft px-2 py-1 text-xs text-success' : 'rounded-full bg-warning-soft px-2 py-1 text-xs text-warning'}>{status}</span>{status !== 'Settled' && <AppButton size="sm" tone="secondary"><Check className="size-4" /> Mark paid</AppButton>}</div></div>)}</div></AppCard></div>; }
