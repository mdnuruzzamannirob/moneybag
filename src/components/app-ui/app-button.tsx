"use client"
import { LoaderCircle } from "lucide-react"
import { Button } from "@/components/ui/button"
import { cn } from "@/lib/utils"
export type AppButtonProps = React.ComponentProps<typeof Button> & { loading?: boolean; tone?: "primary" | "secondary" | "danger" | "success" }
export function AppButton({ children, className, loading, size = "default", tone = "primary", ...props }: AppButtonProps) { const variant = tone === "secondary" ? "outline" : "default"; const dimensions = size === "icon-xs" ? "!size-7 !px-0" : size === "icon-sm" ? "!size-8 !px-0" : size === "icon" ? "!size-9 !px-0" : size === "icon-lg" ? "!size-10 !px-0" : size === "sm" ? "!h-8 !px-2.5" : "!h-10 !px-4"; return <Button {...props} className={cn(dimensions, "rounded-md bg-clip-border font-medium transition-[transform,background-color,border-color] active:scale-[.98]", tone === "primary" && "!border-primary hover:!border-primary hover:bg-primary/90", tone === "secondary" && "!border-border bg-card hover:!border-border hover:bg-secondary", tone === "success" && "!border-success bg-success text-white hover:!border-success hover:bg-success/90", tone === "danger" && "!border-danger bg-danger text-white hover:!border-danger hover:bg-danger/90 focus-visible:!border-danger focus-visible:ring-danger/20", className)} disabled={props.disabled || loading} size={size} variant={variant}>{loading ? <LoaderCircle className="animate-spin" /> : null}{children}</Button> }
