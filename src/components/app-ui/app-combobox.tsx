"use client"
import { Check, ChevronsUpDown } from "lucide-react"
import { useMemo, useState } from "react"
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"
import { cn } from "@/lib/utils"
import { AppInput } from "./app-input"
import type { AppSelectOption } from "./app-select"
export function AppCombobox({ onValueChange, options, placeholder = "Search and select", value }: { onValueChange?: (value: string | null) => void; options: readonly AppSelectOption[]; placeholder?: string; value?: string | null }) { const [query, setQuery] = useState(""); const selected = options.find((option) => option.value === value); const filtered = useMemo(() => options.filter((option) => String(option.label).toLowerCase().includes(query.toLowerCase())), [options, query]); return <Popover><PopoverTrigger render={<button className="flex h-10 w-full items-center gap-2 rounded-md border border-border bg-card px-3 text-sm outline-none hover:border-border focus:border-primary focus:ring-3 focus:ring-primary/15 data-popup-open:border-primary data-popup-open:ring-3 data-popup-open:ring-primary/15" type="button"><span className={cn("min-w-0 flex-1 truncate text-left", selected ? "text-foreground" : "text-muted-foreground")}>{selected?.label ?? placeholder}</span><ChevronsUpDown className="size-4 text-muted-foreground" /></button>} /><PopoverContent align="start" className="w-(--anchor-width) min-w-64 rounded-lg p-2"><AppInput onChange={(event) => setQuery(event.target.value)} placeholder="Search..." value={query} /><div className="mt-2 max-h-60 space-y-1 overflow-y-auto">{filtered.map((option) => <button className={cn("flex w-full items-center gap-2 rounded-md px-2.5 py-2 text-left text-sm hover:bg-primary/10", value === option.value && "bg-primary/10 text-primary")} key={option.value} onClick={() => onValueChange?.(option.value)} type="button"><span className="min-w-0 flex-1">{option.label}</span>{value === option.value ? <Check className="size-4" /> : null}</button>)}</div></PopoverContent></Popover> }
