"use client"
import type { ReactNode } from "react"
import { useState } from "react"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { cn } from "@/lib/utils"
export type AppSelectOption = { description?: string; disabled?: boolean; icon?: ReactNode; label: ReactNode; value: string }
export type AppSelectProps = Omit<React.ComponentProps<typeof Select>, "children" | "onValueChange"> & { ariaLabel?: string; contentClassName?: string; leading?: ReactNode; onValueChange?: (value: string | null) => void; options: readonly AppSelectOption[]; placeholder?: string; size?: 'sm' | 'default' | 'lg'; triggerClassName?: string }
export function AppSelect({ ariaLabel, contentClassName, leading, onValueChange, options, placeholder = "Select an option", size = 'default', triggerClassName, ...props }: AppSelectProps) { const [internalValue, setInternalValue] = useState<string | null>(typeof props.defaultValue === 'string' ? props.defaultValue : null); const currentValue = typeof props.value === 'string' ? props.value : internalValue; const selectedOption = currentValue ? options.find((option) => option.value === currentValue) : undefined; const selectOption = (value: string | null) => { setInternalValue(value); onValueChange?.(value) }; return <Select {...props} onValueChange={(value) => selectOption(typeof value === "string" ? value : null)}><SelectTrigger aria-label={ariaLabel} className={cn("h-10 min-h-10 w-full gap-2 rounded-md !border-border bg-card px-3 text-foreground shadow-none hover:!border-border focus:!border-primary focus:ring-3 focus:ring-primary/15 focus-visible:!border-primary focus-visible:ring-3 focus-visible:ring-primary/15 data-[size=default]:h-10 data-[size=sm]:h-8 data-popup-open:!border-primary data-popup-open:ring-3 data-popup-open:ring-primary/15", { sm: 'text-xs !h-8 min-h-8', default: 'text-sm', lg: 'text-base !h-11 min-h-11' }[size], triggerClassName)} size={size === 'lg' ? 'default' : size}>{leading ? <span className="shrink-0 text-muted-foreground [&>svg]:size-4">{leading}</span> : null}{selectedOption ? <span className="min-w-0 flex-1 truncate text-left">{selectedOption.label}</span> : <SelectValue className="min-w-0 truncate" placeholder={placeholder} />}</SelectTrigger><SelectContent align="start" alignItemWithTrigger={false} className={cn("w-max min-w-(--anchor-width) max-w-[calc(100vw-2rem)] rounded-lg border border-border bg-popover p-1 shadow-lg ring-0 [&_[data-slot=select-item]]:w-full", contentClassName)} sideOffset={6}>{options.map((option) => <SelectItem className={cn("w-full max-w-none whitespace-normal rounded-md pr-9 focus:bg-primary/10 focus:text-primary **:whitespace-normal", { sm: 'min-h-8 px-2 py-1.5 text-xs', default: 'min-h-10 px-2.5 py-2 text-sm', lg: 'min-h-11 px-3 py-2.5 text-sm' }[size])} disabled={option.disabled} key={option.value} value={option.value}>{option.icon ? <span className="shrink-0 text-muted-foreground [&>svg]:size-4">{option.icon}</span> : null}<span className="min-w-0 flex-1"><span className="block break-words font-medium">{option.label}</span>{option.description ? <span className="mt-0.5 block break-words text-xs font-normal text-muted-foreground">{option.description}</span> : null}</span></SelectItem>)}</SelectContent></Select> }
